# SpreadUP - Crypto Arbitrage Telegram Bot

🚀 Высокопроизводительный Telegram-бот для мониторинга арбитражных спредов между спотовым и фьючерсным рынками криптовалют.

## Возможности

- **3 биржи**: MEXC, Gate.io, BingX
- **WebSocket + REST**: Real-time данные с fallback
- **Мгновенные уведомления**: Задержка < 100ms
- **Кросс-биржевой арбитраж**: Сравнение между биржами
- **Высокая производительность**: uvloop + orjson + async

## Быстрый старт

### Локальный запуск

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/SpreadUP.git
cd SpreadUP

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate на Windows

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
cp .env.example .env
# Отредактировать .env и добавить токен бота

# Запустить
python main.py
```

### Docker

```bash
# Запуск с Docker Compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу, подписаться |
| `/stop` | Остановить мониторинг |
| `/scan` | Сканировать рынок |
| `/top` | Топ-10 спредов |
| `/status` | Статус мониторинга |
| `/subscribe` | Подписаться на уведомления |
| `/unsubscribe` | Отписаться |
| `/settings` | Настройки |
| `/help` | Справка |

## Формат уведомления

```
🔥 АРБИТРАЖНЫЙ СПРЕД ОБНАРУЖЕН!

📊 Актив: BTC/USDT
📈 Спред: 3.47%

💰 Цены:
   Спот (MEXC): $67,245.50
   Фьючерс (Gate): $69,578.30

⏱ Время: 2024-01-15 14:32:15 UTC
⚡ Задержка: 45ms
```

## Архитектура

```
┌─────────────────┐
│  Telegram Bot   │  ← aiogram 3.x
└────────┬────────┘
         │
┌────────▼────────┐
│  Core Engine    │  ← asyncio
└────────┬────────┘
         │
    ┌────┴────┬────────┐
    ▼         ▼        ▼
┌───────┐ ┌───────┐ ┌───────┐
│ MEXC  │ │Gate.io│ │ BingX │  ← WebSocket + REST
└───────┘ └───────┘ └───────┘
```

## Технологии

- **aiogram 3.x** - Telegram Bot API
- **aiohttp** - HTTP клиент и WebSocket
- **uvloop** - Ускорение event loop
- **orjson** - Быстрый JSON парсер
- **structlog** - Логирование
- **pydantic** - Валидация данных

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота | - |
| `SPREAD_THRESHOLD` | Мин. спред % | 3.0 |
| `NOTIFICATION_COOLDOWN_SEC` | Кулдаун уведомлений | 60 |
| `LOG_LEVEL` | Уровень логирования | INFO |

## Лицензия

MIT
